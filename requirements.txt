flask>=3.0.0
librosa>=0.10.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
soundfile>=0.12.0
torch>=2.0.0
torchaudio>=0.8
yt-dlp>=2023.0.0
# Demucs from adefossez fork (lameenc skipped for Python 3.14; we use WAV only)
dora-search
einops
julius>=0.2.3
openunmix
tqdm
# After pip install -r requirements.txt, run:
#   pip install --no-deps 'git+https://github.com/adefossez/demucs.git'